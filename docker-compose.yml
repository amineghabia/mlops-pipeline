services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app

  mlflow:
    build: .
    ports:
      - "5001:5000"
    volumes:
      - .:/app
    command: ["mlflow", "server", "--host", "0.0.0.0", "--port", "5000", "--backend-store-uri", "sqlite:///mlflow.db"]
